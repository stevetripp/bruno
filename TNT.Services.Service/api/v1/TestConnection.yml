info:
  name: TestConnection
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_API_URL}}/v1/TestConnection"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
