info:
  name: ReleaseInfo
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_API_URL}}/v2/releaseinfo?releaseId=1"
  params:
    - name: releaseId
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
