info:
  name: bootstrap
  type: http
  seq: 6

http:
  method: GET
  url: "{{BASE_URL}}/api/mobile/v1/me/bootstrap"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
