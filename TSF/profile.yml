info:
  name: profile
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/api/mobile/v1/me/profile"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
