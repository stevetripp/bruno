info:
  name: shifts
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/api/mobile/v1/me/shifts"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
