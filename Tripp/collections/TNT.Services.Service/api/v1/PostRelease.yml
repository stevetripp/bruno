info:
  name: PostRelease
  type: http
  seq: 4

http:
  method: POST
  url: "{{BASE_API_URL}}/v1/postrelease"
  body:
    type: json
    data: |-
      {
        "releaseId": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
