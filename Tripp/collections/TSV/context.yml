info:
  name: context
  type: http
  seq: 5

http:
  method: GET
  url: "{{BASE_URL}}/api/mobile/v1/me/context"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
