info:
  name: health
  type: http
  seq: 9

http:
  method: GET
  url: "{{BASE_URL}}/api/mobile/v1/me/context"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
