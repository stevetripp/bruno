info:
  name: profile PATCH
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{BASE_URL}}/api/mobile/v1/me/profile"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
