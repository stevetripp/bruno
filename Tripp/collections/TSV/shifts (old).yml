info:
  name: shifts (old)
  type: http
  seq: 7

http:
  method: GET
  url: "{{BASE_URL}}/api/mobile/v1/me/shifts/my:19512"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
