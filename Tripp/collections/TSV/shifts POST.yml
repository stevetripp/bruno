info:
  name: shifts POST
  type: http
  seq: 8

http:
  method: POST
  url: "{{BASE_URL}}/api/mobile/v1/me/shifts/{shiftId}/accept"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
