info:
  name: /temples/substitution/enrollment/{templeOrgId}/{workerId}
  type: http
  seq: 4

http:
  method: GET
  url: "{{CORE_WS_URL}}/temples/substitution/enrollment/{{TEMPLE_ORG_ID}}/{{WORKER_ID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
