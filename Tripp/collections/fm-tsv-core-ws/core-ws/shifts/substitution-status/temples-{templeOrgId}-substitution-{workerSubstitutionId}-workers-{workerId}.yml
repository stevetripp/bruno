info:
  name: /temples/{templeOrgId}/substitution/{workerSubstitutionId}/workers/{workerId}
  type: http
  seq: 3

http:
  method: PUT
  url: "{{CORE_WS_URL}}/temples/{{TEMPLE_ORG_ID}}/substitution/{workerSubstitutionId}/workers/{workerId}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
