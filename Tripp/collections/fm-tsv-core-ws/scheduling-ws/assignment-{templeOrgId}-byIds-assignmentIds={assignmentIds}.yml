info:
  name: /assignment/{templeOrgId}/byIds?assignmentIds={assignmentIds}
  type: http
  seq: 4

http:
  method: GET
  url: "{{SCHEDULING_WS_URL}}/assignment/{{TEMPLE_ORG_ID}}/byIds?assignmentIds="
  params:
    - name: assignmentIds
      value: ""
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
