info:
  name: /schedule/{templeOrgId}/shift/{shiftId}/{gender}/{shiftDate}?userPersonUuid={userPersonUuid}
  type: http
  seq: 3

http:
  method: GET
  url: "{{SCHEDULING_WS_URL}}/schedule/{{TEMPLE_ORG_ID}}/shift/39911/ALL/2026-05-04?userPersonUuid={{PERSON_UUID}}"
  params:
    - name: userPersonUuid
      value: "{{PERSON_UUID}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
