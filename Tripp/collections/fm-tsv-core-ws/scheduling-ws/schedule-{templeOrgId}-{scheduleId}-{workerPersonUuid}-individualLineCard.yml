info:
  name: /schedule/{templeOrgId}/{scheduleId}/{workerPersonUuid}/individualLineCard
  type: http
  seq: 2

http:
  method: GET
  url: "{{SCHEDULING_WS_URL}}/schedule/{{TEMPLE_ORG_ID}}/2/{{PERSON_UUID}}/individualLineCard"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
