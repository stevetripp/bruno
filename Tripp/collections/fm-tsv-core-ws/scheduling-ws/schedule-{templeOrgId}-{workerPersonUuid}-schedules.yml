info:
  name: /schedule/{templeOrgId}/{workerPersonUuid}/schedules
  type: http
  seq: 1

http:
  method: GET
  url: "{{SCHEDULING_WS_URL}}/schedule/{{TEMPLE_ORG_ID}}/{{PERSON_UUID}}/schedules"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
