info:
  name: /temples/{templeOrgId}/workers/{workerId}
  type: http
  seq: 2

http:
  method: GET
  url: "{{CORE_WS_URL}}/temples/{{TEMPLE_ORG_ID}}/workers/{{WORKER_ID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
